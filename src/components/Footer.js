import React from 'react';
import { StyledFooter } from './styledcomps/footerStyles';

const Footer = () => {
  return (
    <StyledFooter>
      {/* Footer content */}
    </StyledFooter>
  );
};

export default Footer;
